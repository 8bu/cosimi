/**
 * Phase I5 stub. Real implementation in I7 (satori + @resvg/resvg-js).
 * Until I7 lands, this is a no-op so `pnpm build` (which runs `prebuild`)
 * doesn't fail.
 */
console.log("[og:generate] stub - implementation lands in I7");
